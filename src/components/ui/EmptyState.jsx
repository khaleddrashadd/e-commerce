const EmptyState = ({ title }) => {
  return (
    <div className="flex items-center justify-center text-neutral-600 text-xl font-semibold">
      {title}
    </div>
  );
};
export default EmptyState;
